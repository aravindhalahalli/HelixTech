import React, { Fragment } from 'react';
import AddCourse from '../../components/AddCourse/AddCourse';

const Add = () => {
  return (
    <Fragment>
      <AddCourse />
    </Fragment>
  );
};

export default Add;